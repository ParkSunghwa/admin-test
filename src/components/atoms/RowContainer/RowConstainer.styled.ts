'use client';

import { Box } from '@Atoms/Box';
import styled from 'styled-components';

export const BaseRowContainer = styled(Box)`
    flex-direction: row;
    align-items: start;
    justify-content: space-between;
    width: 100%;
`;
